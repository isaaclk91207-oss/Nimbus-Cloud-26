/**
 * SectionDivider
 * A thin horizontal gradient line used to visually separate sections.
 */
export default function SectionDivider() {
  return <div className="section-divider" aria-hidden="true" />
}
